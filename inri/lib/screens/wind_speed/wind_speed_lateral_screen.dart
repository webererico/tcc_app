import 'package:flutter/material.dart';
import 'package:inri/components/custom_scaffold.dart';

class WindSpeedLateralScreen extends StatelessWidget {
  static const routeName = '/wind-speed-lateral-screen';

  const WindSpeedLateralScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return CustomScaffold(
      title: 'Wind Speed Lateral',
      body: Container(),
    );
  }
}
